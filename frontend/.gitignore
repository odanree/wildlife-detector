node_modules
dist
*.log
.DS_Store
# Bun keeps a text lockfile (bun.lock) which we DO want committed for
# reproducible installs. The binary bun.lockb is legacy — ignore it if it
# ever appears (older Bun versions).
bun.lockb
